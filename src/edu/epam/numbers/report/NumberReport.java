package edu.epam.numbers.report;

import edu.epam.numbers.entity.NumberArray;

public class NumberReport {
    public void printEvenReport(boolean flag, NumberArray array) {
        System.out.println(flag + " .... " + array);
    }
}
